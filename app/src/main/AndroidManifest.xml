<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="designpattern.hjq.hbg.www.com.designpattern">

    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- WIFI权限 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AbstractFactoryPatternActivity"></activity>
        <activity android:name=".AdapterPatternActivity"></activity>
        <activity android:name=".BridgePatternActivity"></activity>
        <activity android:name=".BuilderPatternActivity"></activity>
        <activity android:name=".FactoryPatternActivity"></activity>
        <activity android:name=".IteratorPatternActivity"></activity>
        <activity android:name=".PrototypePatternActivity"></activity>
        <activity android:name=".SingletonPatternActivity"></activity>
        <activity android:name=".TemplatePatternActivity"></activity>
        <activity android:name=".UserGridView"></activity>
        <activity android:name=".MapActivity"></activity>
        <activity android:name=".UiActivity"></activity>
        <activity android:name=".WaterFallActivity"></activity>
    </application>

</manifest>